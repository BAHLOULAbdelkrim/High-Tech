# High-Tech Hugo site

Generated sample Hugo site ready to build. Upload to Cloudflare Pages and set build command `hugo` and publish directory `public`.

Place your real images in `static/images/` replacing placeholders.